{
  "name": "bj-base",
  "version": "1.0.0",
  "description": "Core components and functions to generate funnel components for the BuyerJourneyJS project",
  "main": "src/index.js",
  "module": "src/index.js",
  "scripts": {
    "prod": "esbuild src/index.js --bundle --minify  --outfile=dist/index.min.js",
    "build": "esbuild src/index.js --bundle  --outfile=dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/antoniofregoso/bj-core.git"
  },
  "author": "Antonio Fregoso <antoniofregoso@protonmail.com",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "esbuild": "^0.19.4"
  }
}
